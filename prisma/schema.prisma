// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// type Task {
//   description String // Description of this task of the data processing pipeline
//   outputType  DataType @default(TEXT) // Type of data that comes out of this data processing step
// }

model Project {
  id                 String   @id @default(auto()) @map("_id") @db.ObjectId
  name               String
  owner              String   @default("user_2SH34C1ypE9UWOw9C6WmeffgBX5")
  projectDescription String
  storageUrl         String?
  inputType          DataType @default(TEXT)
  outputType         DataType @default(LABEL)
  classes            String[] @default([])
  taskDescription    String
  createdAt          DateTime @default(now())
  isActive           Boolean  @default(true)
}

enum DataType {
  IMAGE
  IMAGE_MASK
  VIDEO
  TEXT
  LABEL
}
